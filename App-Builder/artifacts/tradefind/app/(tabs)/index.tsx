import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import React from 'react';
import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { BrandMark, JobCard, SectionTitle, TradeCard } from '@/components/TradeFindUI';
import { Trade, useTradeFind } from '@/context/TradeFindContext';
import { useColors } from '@/hooks/useColors';

const categories: { label: string; icon: keyof typeof Ionicons.glyphMap; value: Trade }[] = [{ label: 'Builder', icon: 'business-outline', value: 'Builder' }, { label: 'Electrician', icon: 'flash-outline', value: 'Electrician' }, { label: 'Plumber', icon: 'water-outline', value: 'Plumber' }, { label: 'Painter', icon: 'color-fill-outline', value: 'Painter & Decorator' }, { label: 'Carpenter', icon: 'hammer-outline', value: 'Carpenter' }];
export default function HomeScreen() {
  const colors = useColors(); const insets = useSafeAreaInsets(); const { currentUser, jobs, users } = useTradeFind(); const isTrade = currentUser?.role === 'tradesperson';
  return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={{ paddingTop: insets.top + 16, paddingHorizontal: 18, paddingBottom: insets.bottom + 88 }}><View style={styles.top}><BrandMark compact /><Pressable onPress={() => router.push('/settings')} style={[styles.bell, { backgroundColor: colors.card, borderColor: colors.border }]}><Ionicons name="notifications-outline" size={20} color={colors.foreground} /></Pressable></View><Text style={[styles.greeting, { color: colors.mutedForeground }]}>Good morning,</Text><Text style={[styles.name, { color: colors.foreground }]}>{currentUser?.name}</Text><View style={[styles.hero, { backgroundColor: colors.charcoal }]}><View style={{ flex: 1 }}><Text style={styles.eyebrow}>{isTrade ? 'READY FOR YOUR NEXT JOB?' : 'GET IT DONE RIGHT'}</Text><Text style={styles.heroTitle}>{isTrade ? 'Local work starts here.' : 'Find the right trade for the job.'}</Text><Text style={styles.heroBody}>{isTrade ? 'Browse nearby jobs and connect directly with customers.' : 'Post your job for free and hear from trusted local tradespeople.'}</Text></View><View style={styles.heroIcon}><Ionicons name={isTrade ? 'briefcase' : 'home'} size={27} color="#A6E5B9" /></View></View><View style={styles.actions}><Pressable onPress={() => router.push('/(tabs)/jobs')} style={[styles.action, { backgroundColor: colors.primary }]}><Ionicons name="search" size={20} color={colors.primaryForeground} /><Text style={[styles.actionText, { color: colors.primaryForeground }]}>{isTrade ? 'Find work' : 'Find trades'}</Text></Pressable><Pressable onPress={() => router.push('/(tabs)/post-job')} style={[styles.action, { backgroundColor: colors.card, borderColor: colors.border, borderWidth: 1 }]}><Ionicons name="add-circle-outline" size={20} color={colors.primary} /><Text style={[styles.actionText, { color: colors.foreground }]}>Post a job</Text></Pressable></View><SectionTitle title="Trade categories" action="View all" onAction={() => router.push('/(tabs)/jobs')} /><ScrollView horizontal showsHorizontalScrollIndicator={false}>{categories.map((item) => <Pressable key={item.value} onPress={() => router.push({ pathname: '/(tabs)/jobs', params: { trade: item.value } })} style={styles.category}><View style={[styles.categoryIcon, { backgroundColor: colors.accent }]}><Ionicons name={item.icon} size={21} color={colors.primary} /></View><Text style={[styles.categoryText, { color: colors.foreground }]}>{item.label}</Text></Pressable>)}</ScrollView>{isTrade ? <><SectionTitle title="Jobs near you" action="See all" onAction={() => router.push('/(tabs)/jobs')} />{jobs.slice(0, 2).map((job) => <JobCard key={job.id} job={job} onPress={() => router.push({ pathname: '/job/[id]', params: { id: job.id } })} />)}</> : <><SectionTitle title="Trusted trades near you" action="See all" onAction={() => router.push('/(tabs)/jobs')} />{users.filter((user) => user.role === 'tradesperson').slice(0, 2).map((user) => <TradeCard key={user.id} user={user} onPress={() => router.push({ pathname: '/trade/[id]', params: { id: user.id } })} />)}</>}<View style={[styles.tip, { backgroundColor: colors.accent }]}><Ionicons name="shield-checkmark-outline" size={21} color={colors.primary} /><Text style={[styles.tipText, { color: colors.accentForeground }]}>Normal jobs are always free for tradespeople.</Text></View></ScrollView>;
}
const styles = StyleSheet.create({ top: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 25 }, bell: { width: 40, height: 40, borderRadius: 14, borderWidth: 1, alignItems: 'center', justifyContent: 'center' }, greeting: { fontFamily: 'Inter_500Medium', fontSize: 14 }, name: { fontFamily: 'Inter_700Bold', fontSize: 26, letterSpacing: -0.8, marginTop: 3 }, hero: { borderRadius: 22, padding: 19, marginTop: 18, flexDirection: 'row' }, eyebrow: { color: '#9EDDB1', fontFamily: 'Inter_700Bold', fontSize: 10, letterSpacing: 1 }, heroTitle: { color: '#FFFFFF', fontFamily: 'Inter_700Bold', fontSize: 22, lineHeight: 27, marginTop: 8, maxWidth: 245 }, heroBody: { color: '#B9C9BE', fontFamily: 'Inter_400Regular', fontSize: 12, lineHeight: 18, marginTop: 7, maxWidth: 245 }, heroIcon: { width: 53, height: 53, borderRadius: 18, backgroundColor: '#234A31', alignItems: 'center', justifyContent: 'center' }, actions: { flexDirection: 'row', gap: 10, marginTop: 13 }, action: { flex: 1, height: 51, borderRadius: 15, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 8 }, actionText: { fontFamily: 'Inter_700Bold', fontSize: 14 }, category: { width: 78, alignItems: 'center', marginRight: 8 }, categoryIcon: { width: 51, height: 51, borderRadius: 17, alignItems: 'center', justifyContent: 'center', marginBottom: 7 }, categoryText: { fontFamily: 'Inter_500Medium', fontSize: 11, textAlign: 'center' }, tip: { marginTop: 14, borderRadius: 17, padding: 15, flexDirection: 'row', gap: 11, alignItems: 'center' }, tipText: { flex: 1, fontFamily: 'Inter_500Medium', fontSize: 12 } });
