import { Feather, Ionicons } from '@expo/vector-icons';
import React, { ReactNode } from 'react';
import { Image, Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { Job, User } from '@/context/TradeFindContext';
import { useColors } from '@/hooks/useColors';

export function BrandMark({ compact = false }: { compact?: boolean }) { const colors = useColors(); return <View style={s.brandRow}><View style={[s.brandIcon, { backgroundColor: colors.primary }]}><Feather name="check" size={compact ? 15 : 18} color={colors.primaryForeground} /></View><Text style={[s.brandText, { color: colors.foreground, fontSize: compact ? 18 : 22 }]}>Trade<Text style={{ color: colors.primary }}>Find</Text></Text></View>; }
export function ScreenHeader({ title, subtitle, right }: { title: string; subtitle?: string; right?: ReactNode }) { const colors = useColors(); return <View style={s.header}><View style={{ flex: 1 }}><Text style={[s.headerTitle, { color: colors.foreground }]}>{title}</Text>{subtitle ? <Text style={[s.headerSubtitle, { color: colors.mutedForeground }]}>{subtitle}</Text> : null}</View>{right}</View>; }
export function SectionTitle({ title, action, onAction }: { title: string; action?: string; onAction?: () => void }) { const colors = useColors(); return <View style={s.section}><Text style={[s.sectionTitle, { color: colors.foreground }]}>{title}</Text>{action && onAction ? <Pressable onPress={onAction}><Text style={[s.actionText, { color: colors.primary }]}>{action}</Text></Pressable> : null}</View>; }
export function PrimaryButton({ label, onPress, icon, secondary = false, disabled = false }: { label: string; onPress: () => void; icon?: keyof typeof Ionicons.glyphMap; secondary?: boolean; disabled?: boolean }) { const colors = useColors(); return <Pressable testID={label.replace(/\s/g, '-').toLowerCase()} onPress={onPress} disabled={disabled} style={({ pressed }) => [s.button, { backgroundColor: secondary ? colors.secondary : colors.primary, opacity: disabled ? 0.45 : pressed ? 0.8 : 1 }]}>{icon ? <Ionicons name={icon} size={18} color={secondary ? colors.secondaryForeground : colors.primaryForeground} /> : null}<Text style={[s.buttonText, { color: secondary ? colors.secondaryForeground : colors.primaryForeground }]}>{label}</Text></Pressable>; }
export function Chip({ label, selected, onPress }: { label: string; selected?: boolean; onPress?: () => void }) { const colors = useColors(); return <Pressable onPress={onPress} style={[s.chip, { backgroundColor: selected ? colors.primary : colors.background, borderColor: selected ? colors.primary : colors.border }]}><Text style={[s.chipText, { color: selected ? colors.primaryForeground : colors.mutedForeground }]}>{label}</Text></Pressable>; }
export function SearchField({ value, onChangeText, placeholder }: { value: string; onChangeText: (text: string) => void; placeholder: string }) { const colors = useColors(); return <View style={[s.search, { backgroundColor: colors.card, borderColor: colors.border }]}><Ionicons name="search" size={20} color={colors.mutedForeground} /><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor={colors.mutedForeground} style={[s.searchInput, { color: colors.foreground }]} /></View>; }
export function Stars({ rating, size = 14 }: { rating: number; size?: number }) { const colors = useColors(); return <View style={s.stars}>{[0, 1, 2, 3, 4].map((i) => <Ionicons key={i} name={i < Math.round(rating) ? 'star' : 'star-outline'} size={size} color={colors.amber} />)}<Text style={[s.rating, { color: colors.foreground }]}>{rating ? rating.toFixed(1) : 'New'}</Text></View>; }
export function JobCard({ job, onPress }: { job: Job; onPress: () => void }) { const colors = useColors(); return <Pressable onPress={onPress} style={({ pressed }) => [s.card, { backgroundColor: colors.card, borderColor: colors.border, opacity: pressed ? 0.86 : 1 }]}><View style={s.cardTop}><View style={[s.tradeIcon, { backgroundColor: colors.accent }]}><Ionicons name="construct-outline" size={18} color={colors.primary} /></View><View style={{ flex: 1 }}><Text style={[s.jobTitle, { color: colors.foreground }]} numberOfLines={1}>{job.title}</Text><Text style={[s.meta, { color: colors.mutedForeground }]}>{job.trade} · {job.location}</Text></View><Ionicons name="chevron-forward" size={18} color={colors.mutedForeground} /></View><Text style={[s.description, { color: colors.mutedForeground }]} numberOfLines={2}>{job.description}</Text><View style={s.metaRow}><Text style={[s.metaStrong, { color: colors.foreground }]}>{job.distance}</Text><Text style={[s.meta, { color: colors.mutedForeground }]}>{job.preferredDate}</Text>{job.budget ? <Text style={[s.metaStrong, { color: colors.primary }]}>{job.budget}</Text> : null}</View><View style={s.tagRow}><View style={[s.tag, { backgroundColor: colors.accent }]}><Text style={[s.tagText, { color: colors.accentForeground }]}>{job.urgency}</Text></View><Text style={[s.meta, { color: colors.mutedForeground }]}>{job.postedAt}</Text></View></Pressable>; }
export function TradeCard({ user, onPress }: { user: User; onPress: () => void }) { const colors = useColors(); return <Pressable onPress={onPress} style={[s.card, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={s.cardTop}><View style={[s.avatar, { backgroundColor: colors.secondary }]}>{user.avatar ? <Image source={{ uri: user.avatar }} style={s.avatar} /> : <Text style={[s.avatarText, { color: colors.primary }]}>{user.name.slice(0, 1)}</Text>}</View><View style={{ flex: 1 }}><Text style={[s.jobTitle, { color: colors.foreground }]}>{user.name}</Text><Text style={[s.meta, { color: colors.primary }]}>{user.trade}</Text><Text style={[s.meta, { color: colors.mutedForeground }]}>{user.location}</Text></View><Ionicons name="chevron-forward" size={18} color={colors.mutedForeground} /></View><View style={s.metaRow}><Stars rating={user.rating} /><Text style={[s.meta, { color: colors.mutedForeground }]}>{user.availability}</Text></View></Pressable>; }
export function EmptyState({ icon, title, body }: { icon: keyof typeof Ionicons.glyphMap; title: string; body: string }) { const colors = useColors(); return <View style={[s.empty, { backgroundColor: colors.card, borderColor: colors.border }]}><Ionicons name={icon} size={30} color={colors.primary} /><Text style={[s.emptyTitle, { color: colors.foreground }]}>{title}</Text><Text style={[s.emptyBody, { color: colors.mutedForeground }]}>{body}</Text></View>; }
export function Field({ label, value, onChangeText, placeholder, multiline = false }: { label: string; value: string; onChangeText: (text: string) => void; placeholder?: string; multiline?: boolean }) { const colors = useColors(); return <View style={s.field}><Text style={[s.fieldLabel, { color: colors.foreground }]}>{label}</Text><TextInput value={value} onChangeText={onChangeText} placeholder={placeholder} placeholderTextColor={colors.mutedForeground} multiline={multiline} textAlignVertical={multiline ? 'top' : 'center'} style={[s.input, multiline && s.multiline, { color: colors.foreground, borderColor: colors.border, backgroundColor: colors.card }]} /></View>; }
const s = StyleSheet.create({ brandRow: { flexDirection: 'row', alignItems: 'center', gap: 9 }, brandIcon: { width: 32, height: 32, borderRadius: 10, alignItems: 'center', justifyContent: 'center' }, brandText: { fontFamily: 'Inter_700Bold', letterSpacing: -0.7 }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: 18 }, headerTitle: { fontFamily: 'Inter_700Bold', fontSize: 28, letterSpacing: -0.8 }, headerSubtitle: { fontFamily: 'Inter_400Regular', fontSize: 14, marginTop: 4 }, section: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginTop: 23, marginBottom: 11 }, sectionTitle: { fontFamily: 'Inter_700Bold', fontSize: 18 }, actionText: { fontFamily: 'Inter_600SemiBold', fontSize: 13 }, button: { minHeight: 52, borderRadius: 15, paddingHorizontal: 18, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 9 }, buttonText: { fontFamily: 'Inter_700Bold', fontSize: 15 }, chip: { borderRadius: 100, borderWidth: 1, paddingHorizontal: 13, paddingVertical: 9, marginRight: 8 }, chipText: { fontFamily: 'Inter_600SemiBold', fontSize: 12 }, search: { height: 50, borderRadius: 15, borderWidth: 1, paddingHorizontal: 14, flexDirection: 'row', alignItems: 'center', gap: 9 }, searchInput: { flex: 1, fontFamily: 'Inter_400Regular', fontSize: 15 }, card: { borderRadius: 18, borderWidth: 1, padding: 16, marginBottom: 11 }, cardTop: { flexDirection: 'row', alignItems: 'center', gap: 11 }, tradeIcon: { width: 40, height: 40, borderRadius: 13, alignItems: 'center', justifyContent: 'center' }, avatar: { width: 48, height: 48, borderRadius: 16, alignItems: 'center', justifyContent: 'center' }, avatarText: { fontFamily: 'Inter_700Bold', fontSize: 20 }, jobTitle: { fontFamily: 'Inter_700Bold', fontSize: 16 }, meta: { fontFamily: 'Inter_400Regular', fontSize: 12, marginTop: 3 }, metaStrong: { fontFamily: 'Inter_700Bold', fontSize: 12 }, description: { fontFamily: 'Inter_400Regular', fontSize: 13, lineHeight: 19, marginTop: 13 }, metaRow: { flexDirection: 'row', alignItems: 'center', gap: 13, marginTop: 14 }, tagRow: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginTop: 13 }, tag: { borderRadius: 8, paddingHorizontal: 8, paddingVertical: 5 }, tagText: { fontFamily: 'Inter_600SemiBold', fontSize: 11 }, stars: { flexDirection: 'row', alignItems: 'center', gap: 2 }, rating: { fontFamily: 'Inter_700Bold', fontSize: 12, marginLeft: 4 }, empty: { borderWidth: 1, borderRadius: 18, padding: 28, alignItems: 'center', marginTop: 8 }, emptyTitle: { fontFamily: 'Inter_700Bold', fontSize: 16, marginTop: 12 }, emptyBody: { fontFamily: 'Inter_400Regular', fontSize: 13, lineHeight: 19, textAlign: 'center', marginTop: 6 }, field: { marginBottom: 15 }, fieldLabel: { fontFamily: 'Inter_600SemiBold', fontSize: 13, marginBottom: 7 }, input: { minHeight: 49, borderWidth: 1, borderRadius: 13, paddingHorizontal: 14, fontFamily: 'Inter_400Regular', fontSize: 15 }, multiline: { minHeight: 110, paddingTop: 13 } }); 